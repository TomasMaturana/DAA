Para correr los scripts, tiene que tener las librerias que estan en el archivo requirements.txt. En particular la libreria de numpy.

Para instalar requirements:

```pip install -r requirements.txt```


